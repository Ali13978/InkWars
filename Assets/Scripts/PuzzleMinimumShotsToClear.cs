using UnityEngine;
public class PuzzleMinimumShotsToClear : MonoBehaviour
{
    // Methods
    private void Start()
    {
        string val_5 = Character_GlobalInfo.stage_CurrentLevel + 2.ToString();
        if((this.gameObject.GetComponent<UnityEngine.UI.Text>()) != null)
        {
                return;
        }
    
    }
    public PuzzleMinimumShotsToClear()
    {
    
    }

}
